# Сверка с первоначальным планом и оставшаяся работа

## Статус требований

| Первоначальное требование | Статус | Комментарий |
| --- | --- | --- |
| Читать MIDI и рисовать ноты | Частично | Основной вход — MuseScore `.mscz`; Mac готовит `.pianoscore`, Android показывает точные SVG MuseScore. Автоматическая транскрипция произвольного MIDI прямо на планшете не реализована. |
| Страницы, ручное и автоматическое листание | Готово | Ручные кнопки, авто-переход по курсору и при обучении. |
| Масштаб нот | Готово | Pinch zoom в просмотрщике SVG. |
| Bluetooth MIDI | Готово | BLE MIDI и проверка с Roland FP-30. |
| MIDI по USB | Готово | Системные USB MIDI endpoints. |
| Проигрывание MIDI | Готово для FP-30 | Отправка в подключённое устройство, курсор и подсветка. |
| Ожидающее обучение: всё произведение | Готово | Обе/левая/правая рука. |
| Ожидающее обучение: выбранный сегмент | Готово | Интерактивные границы `От`/`До`. |
| Непрерывное обучение во время Play: всё произведение | Готово, нужна калибровка | Входящие Note On сопоставляются с текущей группой в окне допуска; есть правильные/ошибочные ноты и итог. |
| Непрерывное обучение во время Play: сегмент | Готово, нужна калибровка | Та же оценка применяется к выбранному `От/До` диапазону. |
| Загрузка на планшет без Play Store | Готово | Debug APK через ADB. |

## Приоритетный следующий этап

### 1. Проверка и калибровка на реальном FP-30

- Проверить на FP-30 по BLE и USB: правильная нота, неправильная нота, аккорд, опережение и запаздывание.
- Отдельно проверить быстрые последовательности Note On/Off, педаль и удержание клавиш:
  в приложении теперь разбирается весь MIDI callback, но нужна длительная hardware-проверка.
- Откалибровать текущее окно допуска ±половина четверти; вероятно, добавить отдельные профили BLE/USB и настройку пользователя.
- При необходимости развести «обычный Play» и «Play с оценкой» отдельной кнопкой.
- Добавить в итог правильные/ошибочные/пропущенные ноты и процент, а не только группы.

### 2. Валидация пайплайна на разных произведениях

Точная связь учебного MIDI с SVG MuseScore уже реализована для `Listen to Your Heart`:
817 событий, 459 SVG-сегментов, две явные руки и карта тактов. Следующая работа —
доказать переносимость решения.

- Прогнать несколько произведений с акцидентами, лига́ми, паузами, несколькими
  голосами, сменами ключа/размера/темпа.
- Сделать отчёт конвертации удобнее для ручной музыкальной правки, когда MuseScore
  не даёт ожидаемую фортепианную партию или SVG-привязку.
- При необходимости добавить ручной редактор рук/исключаемых событий в Mac pipeline.

### 3. Надёжность и UX

- Показать устойчивый индикатор подключения и reconnect при засыпании FP-30/BLE.
- Развести действия `Стоп`, `Сбросить обучение`, `Сбросить диапазон` и дать понятные подсказки режима выбора `От/До`.
- Добавить настройку окна аккорда (сейчас 250 мс) и окна допуска в непрерывном режиме.
- Улучшить диапазон: визуальные ручки начала/конца, точное включение последней ноты, сохранение избранных сегментов с именами.
- Запоминать последнюю композицию, масштаб, страницу, режим рук и интервал.
- Продумать небольшой onboarding для BLE/USB FP-30.

### 4. Воспроизведение и звук

- Восстанавливать program/control state перед Play из середины композиции.
- Добавить паузу/продолжение, loop выбранного диапазона и метроном. Управление
  скоростью 0,25×–1,5× уже реализовано.
- При необходимости добавить программный synth как fallback, но для обучения приоритет — локальный звук FP-30 с минимальной задержкой.

### 5. Качество проекта

- Расширить unit tests: tempo map, границы диапазона, mapping validation. Tests для
  WaitingPractice и batch MIDI/running status уже есть.
- Добавить instrumented/UI tests для import, MIDI screen и диапазона.
- Настроить release signing для локальной установки, если debug APK перестанет быть удобен.
- Добавить CI build + lint и версионируемый changelog.

## Не делать до закрытия этапов 1–2

- Самостоятельный Android MIDI→MusicXML/гравировку: это отдельный большой продукт с низкой предсказуемостью качества.
- Bluetooth audio: он добавляет задержку и не нужен при использовании встроенного звука FP-30.
- Публикацию в Play Store: пользователь явно выбрал локальную установку.
